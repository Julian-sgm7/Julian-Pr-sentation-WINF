name: Architektur-Prinzipien Prüfung

on:
  push:
    branches: [ main ]
    paths:
      - 'version*/**'
      - 'templates/**'
      - 'shared-examples/**'
  pull_request:
    branches: [ main ]
    paths:
      - 'version*/**'
      - 'templates/**'
      - 'shared-examples/**'

jobs:
  architecture-check:
    runs-on: ubuntu-latest
    
    steps:
      - name: 📥 Checkout Code
        uses: actions/checkout@v3
      
      - name: 🐍 Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'
      
      - name: 📦 Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install beautifulsoup4 lxml cssutils
      
      - name: 🔍 Architektur-Validierung durchführen
        id: arch_check
        run: |
          python scripts/check_architecture.py
        continue-on-error: true
      
      - name: 📊 Ergebnisse anzeigen
        if: always()
        run: |
          if [ -f architecture_report.md ]; then
            echo "=== Architektur-Prüfbericht ==="
            cat architecture_report.md
          fi
      
      - name: 💬 Kommentar mit Feedback erstellen
        if: github.event_name == 'pull_request' && always()
        uses: actions/github-script@v6
        with:
          script: |
            const fs = require('fs');
            let report = '## 🏛️ Architektur-Prinzipien Prüfung\n\n';
            
            if (fs.existsSync('architecture_report.md')) {
              report += fs.readFileSync('architecture_report.md', 'utf8');
            } else {
              report += '✅ Keine Architektur-Probleme gefunden!\n\n';
              report += 'Alle Prinzipien werden eingehalten. Weiter so! 🚀';
            }
            
            report += '\n\n---\n';
            report += '📚 **Mehr erfahren:** [Architektur-Prinzipien Dokumentation](../blob/main/docs/architektur-prinzipien.md)';
            
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: report
            });
      
      - name: 📤 Upload Bericht als Artifact
        if: always()
        uses: actions/upload-artifact@v3
        with:
          name: architecture-report
          path: |
            architecture_report.md
            architecture_details.json
          if-no-files-found: ignore
      
      - name: ✅ Status setzen
        if: steps.arch_check.outcome == 'failure'
        run: |
          echo "⚠️ Architektur-Prüfung hat Verbesserungsvorschläge gefunden."
          echo "Dies ist KEIN Fehler - nur Hinweise zur Code-Qualität!"
          echo "Siehe Bericht oben für Details."
