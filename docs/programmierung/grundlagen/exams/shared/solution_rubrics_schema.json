{
  "type": "object",
  "required": [
    "schema_version",
    "generated_at",
    "schema_ref",
    "strategy",
    "entries"
  ],
  "additionalProperties": false,
  "properties": {
    "schema_version": {
      "type": "integer",
      "enum": [1]
    },
    "generated_at": {
      "type": "string",
      "minLength": 1
    },
    "schema_ref": {
      "type": "string",
      "minLength": 1
    },
    "strategy": {
      "type": "object",
      "required": ["source", "task_keys", "points_unit", "criterion_ids"],
      "additionalProperties": false,
      "properties": {
        "source": { "type": "string", "minLength": 1 },
        "task_keys": {
          "type": "array",
          "minItems": 4,
          "items": { "type": "string", "enum": ["A", "B", "C", "D"] }
        },
        "points_unit": { "type": "string", "minLength": 1 },
        "criterion_ids": { "type": "string", "minLength": 1 }
      }
    },
    "entries": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": [
          "entry_id",
          "language",
          "theme",
          "variant",
          "task",
          "task_id",
          "points_total",
          "criteria",
          "common_errors",
          "path"
        ],
        "additionalProperties": false,
        "properties": {
          "entry_id": { "type": "string", "minLength": 1 },
          "language": { "type": "string", "minLength": 1 },
          "theme": { "type": "string", "minLength": 1 },
          "variant": { "type": "string", "minLength": 1 },
          "task": { "type": "string", "enum": ["A", "B", "C", "D"] },
          "task_id": { "type": "string", "minLength": 1 },
          "points_total": { "type": "number" },
          "criteria": {
            "type": "array",
            "minItems": 1,
            "items": {
              "type": "object",
              "required": [
                "criterion_index",
                "criterion_key",
                "criterion_id",
                "criterion_family_id",
                "points",
                "criterion",
                "hint"
              ],
              "additionalProperties": false,
              "properties": {
                "criterion_index": { "type": "integer" },
                "criterion_key": { "type": "string", "minLength": 1 },
                "criterion_id": { "type": "string", "minLength": 1 },
                "criterion_family_id": { "type": "string", "minLength": 1 },
                "points": { "type": "number" },
                "criterion": { "type": "string", "minLength": 1 },
                "hint": { "type": "string" }
              }
            }
          },
          "common_errors": {
            "type": "array",
            "minItems": 1,
            "items": { "type": "string", "minLength": 1 }
          },
          "path": { "type": "string", "minLength": 1 }
        }
      }
    }
  }
}
