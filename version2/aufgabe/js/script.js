// TODO: Hamburger-Menü Toggle-Funktionalität implementieren

// 1. Elemente auswählen
// const navToggle = document.getElementById('navToggle');
// const mainNav = document.getElementById('mainNav');

// 2. Event Listener für Toggle-Button
// navToggle.addEventListener('click', function() {
//     // Toggle 'active' Klasse auf Navigation
//     // Toggle 'active' Klasse auf Button (für Animation)
// });

// 3. Menü schließen beim Klick auf Link (Optional)
// const navLinks = mainNav.querySelectorAll('a');
// navLinks.forEach(link => {
//     link.addEventListener('click', () => {
//         // Navigation schließen
//     });
// });

// Beispiel-Lösung (auskommentiert):
/*
const navToggle = document.getElementById('navToggle');
const mainNav = document.getElementById('mainNav');

navToggle.addEventListener('click', function() {
    mainNav.classList.toggle('active');
    navToggle.classList.toggle('active');
});

const navLinks = mainNav.querySelectorAll('a');
navLinks.forEach(link => {
    link.addEventListener('click', () => {
        mainNav.classList.remove('active');
        navToggle.classList.remove('active');
    });
});
*/
